{
  "name": "@brightsteps/spaced-repetition",
  "version": "0.1.0",
  "private": true,
  "author": "Prabhu Kiran Avula",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "format": "prettier --check 'src/**/*.ts'"
  }
}
