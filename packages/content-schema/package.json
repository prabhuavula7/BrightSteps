{
  "name": "@brightsteps/content-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "format": "prettier --check 'src/**/*.ts'"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
